import { DEFAULT_THEME } from '@caldera-digital/theme'

/**
 * Extend the theme here like so...
 *
 * {
 *   ...DEFAULT_THEME,
 *   primaryColor: 'burlywood',
 * }
 */
export const THEME = {
  primaryColor: '#32325D', // Stripe primary
  secondaryColor: '#3ecf8e', // Stripe CTA color from website
  ...DEFAULT_THEME,
}
